[project]
name = "pure-functions"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.pyright]
include = ["."]
executionEnvironments = [{ root = "." }]
extraPaths = ["."]

[tool.ruff]
src = ["."]

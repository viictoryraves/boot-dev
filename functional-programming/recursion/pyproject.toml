[project]
name = "recursion"
version = "0.1.0"
description = "Recursion module from devops path in boo.dev"
readme = "README.md"
requires-python = ">=3.14"

[tool.ruff]
# Indica a Ruff cuál es la raíz para resolver imports
src = ["."]

[tool.pyright]
venvPath = "."
venv = ".venv"
